{
  "name": "@rusty-engine/studio-editor-shell",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "prebuild": "pnpm --filter @rusty-engine/studio-user-settings run build && pnpm --filter @rusty-engine/studio-adapter-client run build && pnpm --filter @rusty-engine/studio-viewport run build && pnpm --filter @rusty-engine/studio-voxel-editor run build",
    "build": "tsc --project tsconfig.build.json",
    "prepare": "pnpm run build",
    "pretypecheck": "pnpm --filter @rusty-engine/studio-user-settings run build && pnpm --filter @rusty-engine/studio-adapter-client run build && pnpm --filter @rusty-engine/studio-viewport run build && pnpm --filter @rusty-engine/studio-voxel-editor run build",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "pnpm run build && node --test 'dist/**/*.test.js'"
  },
  "dependencies": {
    "@angular/common": "~21.2.0",
    "@angular/core": "~21.2.0",
    "@angular/forms": "~21.2.0"
  },
  "peerDependencies": {
    "@rusty-engine/render-contracts": "0.1.0",
    "@rusty-engine/studio-adapter-client": "0.1.0",
    "@rusty-engine/studio-user-settings": "0.1.0",
    "@rusty-engine/studio-viewport": "0.1.0",
    "@rusty-engine/studio-voxel-editor": "0.1.0"
  },
  "devDependencies": {
    "@rusty-engine/render-contracts": "workspace:*",
    "@rusty-engine/studio-adapter-client": "workspace:*",
    "@rusty-engine/studio-user-settings": "workspace:*",
    "@rusty-engine/studio-viewport": "workspace:*",
    "@rusty-engine/studio-voxel-editor": "workspace:*",
    "@types/node": "^24.0.0"
  }
}
