{
  "name": "@rusty-engine/studio-viewport",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "prebuild": "pnpm --dir ../.. run build:renderer",
    "build": "tsc --project tsconfig.build.json",
    "prepare": "pnpm run build",
    "pretypecheck": "pnpm --dir ../.. run build:renderer",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "pnpm run build && node --test 'dist/**/*.test.js'"
  },
  "dependencies": {
    "@angular/core": "~21.2.0"
  },
  "peerDependencies": {
    "@rusty-engine/render-contracts": "0.1.0",
    "@rusty-engine/render-projection": "0.1.0",
    "@rusty-engine/renderer-host": "0.1.0",
    "@rusty-engine/renderer-three": "0.1.0"
  },
  "devDependencies": {
    "@rusty-engine/render-contracts": "workspace:*",
    "@rusty-engine/render-projection": "workspace:*",
    "@rusty-engine/renderer-host": "workspace:*",
    "@rusty-engine/renderer-three": "workspace:*",
    "@types/node": "^24.0.0"
  }
}
